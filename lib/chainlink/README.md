# @chainlink/contracts (vendored interface)

`contracts/src/v0.8/shared/interfaces/AggregatorV3Interface.sol` is copied verbatim (MIT) from the
`@chainlink/contracts@1.3.0` npm package — the version pinned by `@sablier/evm-utils` 2.0.1 — and is
identical in 1.5.0. It exists only so `lib/sablier-evm-monorepo/utils/src/SablierComptroller.sol`
compiles; nothing in `src/` imports it. Remapped as `@chainlink/=lib/chainlink/` in `foundry.toml`.
