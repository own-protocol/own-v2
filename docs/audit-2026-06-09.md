# Own Protocol v2 — Security Audit Report

**Scope:** `full` (all of `src/`)
**Date:** 2026-06-09
**Branch:** `lending`
**Method:** Spec review (AGENTS.md, protocol.md, leverage-design.md) → manual review of all 10 core contracts + 2 libraries + 3 routers, parallel deep-dives, and direct source verification of every HIGH/CRITICAL finding.

---

## Executive Summary

| Severity | Count |
|---|---|
| **Critical** | 1 |
| **High** | 3 |
| **Medium** | 4 |
| **Low** | 5 |
| **Info** | several |

Build compiles clean (only `asm-keccak256` lint notes). The codebase is well-structured and consistently uses SafeERC20, ReentrancyGuard, and custom errors. The ERC-4626 inflation defense, quote replay protection, and lending index math are sound.

**The dominant risk is a single systemic oracle flaw: the inline `verifyPrice` proof path enforces no staleness, which undermines every price-sensitive recourse path — force-redemption, borrowing, and liquidation. Treat C-01 as a release blocker.**

---

## 1. Spec-to-Code Compliance Matrix (highlights)

| Requirement (spec) | Status | Notes |
|---|---|---|
| RFQ market/limit settlement, single-use quotes, chainId+contract binding | **IMPLEMENTED** | OwnMarket.sol:304-338 — verified sound |
| Quote uses **EIP-712** (per docs/AGENTS) | **DIVERGENT** | Actually EIP-191 personal-sign (OwnMarket.sol:333). Functionally safe; doc/UX mismatch |
| Force-exec at **fresh** oracle price | **DIVERGENT** | `verifyPrice` enforces no staleness (see C-01) |
| Global util + per-asset USD cap (O(1)) | **IMPLEMENTED** | VaultManager.sol:119-157 |
| ERC-4626 async deposit/withdraw, halt bypass | **IMPLEMENTED** | OwnVault.sol; FIFO claim is **PARTIAL** (M-04) |
| Lending: Aave-funded borrow/repay/liquidate, debt cap, two-slope rate | **IMPLEMENTED** | BorrowManager.sol |
| Lending fee 3-way split (protocol/LP/VM) | **PARTIAL/DIVERGENT** | Surplus swept 100% to VM (BorrowManager.sol:736); doc says split. Doc flags as planned |
| FeeCalculator / on-chain fees | **MISSING** (by design) | Doc states not yet wired |

---

## 2. Security Findings

### [CRITICAL] C-01 — `verifyPrice` has no staleness/nonce check; stale signed prices drain collateral via force-execute, borrow, and liquidate

- **Contract:** OracleVerifier.sol (+ PythOracleVerifier.sol)
- **Function:** `verifyPrice()`
- **Line:** 122-136

**Description:** The push path `updatePrice` enforces staleness (line 70), but the inline-proof path `verifyPrice` only checks `price != 0` and signature validity — **no staleness, no expiry, no sequence number**. Any price the signer has *ever* signed for an asset is a valid proof forever. The consumers all pass **caller-supplied** `priceData`:
- `OwnMarket.forceExecuteOrder` (OwnMarket.sol:202, 207) — redeemer supplies asset + collateral proofs.
- `BorrowManager.borrow / liquidate / absorbBadDebt` (BorrowManager.sol:199, 306, 407) — borrower/liquidator supplies the price.

`PythOracleVerifier.verifyPrice` has the same class of gap: it trusts the caller's `[minPublishTime, maxPublishTime]` window and never enforces `maxPriceAge` on the inline path.

**Impact:** Direct theft of LP collateral via multiple vectors:
- **Force-redeem:** pick highest-ever asset price + lowest-ever collateral price → `grossCollateral = remaining·assetPrice/collateralPrice` releases far more vault collateral than the redemption is worth.
- **Borrow:** supply a stale *high* eToken price → `collateralValueUSD` inflated → borrow stablecoins against overvalued collateral, walk away underwater. LPs eat the shortfall.
- **Liquidate:** supply a stale *low* price → make a healthy position look liquidatable → seize collateral + bonus.

Preconditions are minimal (hold a redeem order and wait `claimThreshold`; or just call `borrow`). Affects every asset regardless of oracle type.

**Proof of Concept:** (1) Signer publishes eTSLA @ $500 at T; signature S is on-chain/in logs. (2) Price falls to $300. (3) Attacker `forceExecuteOrder(orderId, vault, encode($500,T,S), encode(lowEthPrice,...))` → `verifyPrice` returns $500, timestamp ignored → ~40% over-release.

**Recommendation:** In `verifyPrice`, enforce per-asset staleness: `if (block.timestamp - timestamp > maxStaleness) revert StalePrice()`; add a per-asset monotonic sequence/nonce to the signed digest to invalidate superseded prices. In `PythOracleVerifier`, clamp `maxPublishTime` to `block.timestamp` and require `block.timestamp - publishTime <= maxPriceAge`. Have `OwnMarket`/`BorrowManager` assert the returned timestamp's freshness.

---

### [HIGH] H-01 — `forceExecuteOrder` accepts a halted (wind-down) vault as collateral source

- **Contract:** OwnMarket.sol
- **Function:** `forceExecuteOrder()`
- **Line:** 189, 214 (sink: OwnVault.sol:537)

**Description:** The only check on the caller-named `vault` is `isRegisteredVault` (line 189). A `Halted` vault has had its collateral excluded from the global pool (`onVaultHalted`, VaultManager.sol:256) and is winding down to its LPs, yet `releaseCollateral` (OwnVault.sol:537, `onlyMarket`, no status check) will still pay out from it, while `closeExposure` reduces the *global* book.

**Impact:** A redeemer cherry-picks the wind-down vault and drains collateral reserved for that vault's instant LP withdrawals; loss/dilution mutualized onto the halted vault's LPs beyond design intent.

**Recommendation:** In `forceExecuteOrder` require `IOwnVault(vault).vaultStatus() == VaultStatus.Active` and/or `!vmgr.isVaultExcluded(vault)`.

---

### [HIGH] H-02 — Resting redeem escrow is stranded after `migrateToken` (stock split)

- **Contract:** OwnMarket.sol
- **Function:** `placeOrder`/`cancelOrder`/`expireOrder`/`fillOrder`/`forceExecuteOrder` via `_activeToken`
- **Line:** 128 (escrow) vs 215/239/390/400 (settle)

**Description:** A redeem order escrows the eToken resolved at placement (line 128). Every later path re-resolves `_activeToken(asset)`. `AssetRegistry.migrateToken` (AssetRegistry.sol:74-83) swaps `activeToken`. After a migration, the contract holds the *old* token but tries to transfer/burn the *new* one → revert or wrong-asset burn. Escrowed legacy eTokens become unrecoverable.

**Impact:** Permanent loss of user escrow on any redeem order open across a stock-split migration; `cancelOrder`/`expireOrder` revert (funds locked forever).

**Recommendation:** Snapshot the escrowed token address in the `Order` struct at `placeOrder` and use it for all returns/burns, instead of re-resolving.

---

### [HIGH] H-03 — Withdrawal utilization gate combines a stale cached mark with live `totalAssets`

- **Contract:** VaultManager.sol / OwnVault.sol
- **Function:** `withdrawalBreachesUtil()`
- **Line:** VaultManager.sol:466-474; OwnVault.sol:359

**Description:** `withdrawalBreachesUtil` nets against `_globalCollateralUSD` (sum of cached marks). After `releaseCollateralForBadDebt` (OwnVault.sol:544) shrinks real `totalAssets` with **no** mark refresh (the code comment admits "catches up on the next keeper price pull"), `_globalCollateralUSD` is overstated, so the solvency gate reads utilization lower than reality and lets LPs exit ahead of socialized losses.

**Impact:** Informed LPs race out at a stale-favorable utilization snapshot; remaining LPs absorb a larger share; protocol can be pushed past its true solvency cap while the gate still passes.

**Recommendation:** Refresh `pullCollateralPrice(vault)` at the start of `fulfillWithdrawal`, and decrement `_collateralMark`/`_globalCollateralUSD` synchronously inside `releaseCollateralForBadDebt`. Never combine a cached mark with live `totalAssets` in a solvency-blocking check.

---

### [MEDIUM] M-01 — Pyth confidence interval ignored
PythOracleVerifier `_normalizePythPrice` uses only `price`/`expo`, never `conf`. Wide-uncertainty prices are consumed as exact. **Fix:** reject if `conf·BPS/price > maxConfBps`.

### [MEDIUM] M-02 — In-house push path applies no bounds when an asset's oracle config is unset
OracleVerifier.sol:70,79 — staleness and deviation are gated on `>0` config values; an asset never configured via `setAssetOracleConfig` accepts a single unbounded price. **Fix:** require a configured non-zero `maxStaleness`/`maxDeviation` before accepting prices.

### [MEDIUM] M-03 — `setRateParams` / interest params unvalidated → accrual DoS
BorrowManager.sol:528 + `InterestRateModel.premium` reverts if `optimalUtilBps==0 || >=BPS`. Bad params (or an unvalidated constructor `rateParams_`) make `_accrue` revert, which bricks **repay and liquidate** too (they must stay open). **Fix:** validate params on set and in constructor.

### [MEDIUM] M-04 — Withdrawal queue is not FIFO despite the spec
OwnVault.sol:339 — `fulfillWithdrawal` enforces only wait period + util gate; no ordering. Near the cap, limited capacity is allocated by gas-race/caller choice, letting later LPs jump ahead. **Fix:** enforce oldest-pending-first, or drop the FIFO claim from the spec.

---

### [LOW]

- **L-01** Quote digest is EIP-191, not EIP-712 as documented (OwnMarket.sol:304-321) — spec/UX mismatch, no fund risk.
- **L-02** Settlement reverts for payment tokens with >18 decimals and has no fee-on-transfer guard (OwnMarket.sol:235,356,385). `setPaymentToken` does check `decimals()<=18` (VaultManager.sol:358), so mostly mitigated; assert no-fee-on-transfer.
- **L-03** `closeExposure` lacks the `mark != 0` guard that `openExposure` has (VaultManager.sol:149) — latent asymmetry; not currently reachable.
- **L-04** `_borrowManager` is one-shot with no rotation (OwnVault.sol:506) — a compromised borrow manager can only be handled via `haltVault`. Note safe-by-construction: delegation beneficiary and bad-debt sink are fixed.
- **L-05** `migrateToken` allows `newToken == activeToken` / re-adding a legacy token (AssetRegistry.sol:74); `updateAssetConfig` silently drops `active`/`activeToken` fields (AssetRegistry.sol:51) — operator footguns.

### [INFO]

- EToken pass-through dividend redirect (EToken.sol `_update`) may misattribute dividends for multi-beneficiary custodians; `setPassThroughHolder` is admin-griefable. **Recommend confirming intended direction + multi-beneficiary tests** (not fully verified — flag for follow-up).
- Lending premium swept 100% to VM, not the documented 3-way split (BorrowManager.sol:736).
- CEI ordering in `BorrowManager.borrow/repay` does external calls before final state writes; safe only because of `nonReentrant` — tighten to true CEI.
- `OracleVerifier.verifyPriceForSession` uses an external `this.verifyPrice` self-call dropping `msg.value` (line 145).
- Routers (Aave/Wst/WETH) verified clean: stateless, no trapped funds, slippage enforced; consider `isRegisteredVault` checks for defense-in-depth.
- ProtocolRegistry 2-day timelock verified correctly enforced; cannot be bypassed/shortened.

---

## 2A. Addendum — findings from subsequent re-audits (2026-06-11)

Discovered after this report during focused re-audits (BorrowManager + AaveRouter, and the H-02
migration remediation). Remediation details for fixed items live in
[`audit-fixes-2026-06-10.md`](./audit-fixes-2026-06-10.md).

- **[HIGH] H-04 — `absorbBadDebt` releases collateral in 18-dec units, not the vault's native
  decimals.** For a <18-decimal collateral vault (aUSDC/USDC) the released amount is 10^(18−dec)×
  too large → the transfer reverts (or over-releases if balance allowed), bricking bad-debt
  absorption. **Status: Fixed** (native scale-down via a `BorrowManager._convertToCollateral` helper).
- **[MEDIUM] M-05 — Interest-model divergence vs. real Aave debt.** Sampled simple-interest book debt
  can lag Aave's continuously-compounding debt during a rate spike → borrowers under-cover the Aave
  loan → LP shortfall. **Status: Fixed** (interest index floored to `debtToken.balanceOf(vault)`;
  permissionless `accrue()` keeper sync added).
- **[LOW] L-06 — `BorrowManager.sweepDividends(eToken)` does not validate the token address.** Cannot
  enrich the caller (proceeds always go to the VM) and cannot touch pooled collateral, but should
  assert the token is the active or a legacy token of a registered asset
  (`AssetRegistry.isValidToken`). **Status: Open.**
- **[LOW] L-07 — `migrateToken` + `VaultManager.applySplit` are not atomic.** Skipping `applySplit`
  leaves converted tokens temporarily unredeemable (liveness only, admin-recoverable); enforce a
  single multisig batch (or an on-chain coordinator). **Status: Open.**
- The `migrateToken` `newToken == oldToken` guard suggested in the re-audit is already covered by
  **L-05** above.

**Status of the original findings:** C-01, H-01, H-02, H-03 — **Fixed**. M-01–M-04 and L-01–L-05 —
**Open**. (Full remediation log: `audit-fixes-2026-06-10.md`.)

---

## 3. Code Quality & Simplification

- ERC-4626 inflation defense (`_decimalsOffset()=6`), `totalAssets` excluding `_pendingDepositAssets`, and rounding directions are correct.
- `deposit()`/`mint()` access asymmetry (OwnVault.sol:151-178): `mint` is always `onlyManager` while `deposit` is open when approval not required — standard ERC-4626 `mint` integrators revert. Align or document.
- Apply the `asm-keccak256` lint or suppress with a config note (OracleVerifier.sol:64,133; OwnMarket.sol:307).

## 4. Gas Optimizations

- `_consumeQuote` recomputes `quoteDigest` (abi.encode of 10 fields) — fine, but cache `registry.vaultManager()` (read 2×).
- Settlement calls `IERC20Metadata(pay).decimals()` per settlement — cache payment-token decimals once at VaultManager set-time.
- `verifyPriceForSession` external self-call wastes gas; refactor to internal.
- Minor; **do not pursue before fixing C-01/H-01.**

## 5. Test Gap Analysis (priority)

1. **C-01:** test `verifyPrice` rejects stale timestamps; force-execute/borrow/liquidate with an old-but-signed price must revert. No such test exists.
2. **H-01:** `forceExecuteOrder` against a `Halted` vault must revert.
3. **H-02:** redeem order placed → `migrateToken` → `cancelOrder` must still return the original escrow.
4. **H-03:** invariant: `releaseCollateralForBadDebt` then `fulfillWithdrawal` cannot exceed true utilization.
5. **M-03:** `setRateParams` with `optimalUtilBps=0` then `repay` must not brick.

## 6. Recommended Actions (priority order)

1. **Fix C-01** — add staleness + nonce to `verifyPrice` (in-house) and enforce `maxPriceAge` on the Pyth inline path. Single highest-impact fix; closes theft vectors in force-redeem, borrow, and liquidate simultaneously.
2. **Fix H-01** (vault-status check), **H-02** (snapshot escrow token), **H-03** (synchronous mark update on bad-debt / refresh on withdraw).
3. Address M-01…M-04 (Pyth confidence, oracle-config requirement, rate-param validation, FIFO).
4. Resolve the EToken pass-through INFO (confirm design), then Low/Info hygiene.

---

*Findings cite `file:line` at the time of review (branch `lending`, commit `fa9cf33`). Every Critical/High was verified by reading the source directly.*
