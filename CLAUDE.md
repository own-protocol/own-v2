# Own V2

## Primary Knowledge

Read `AGENTS.md` for full project context, architecture, coding conventions, and development instructions. It is the primary source of truth for this project.

## Implementation Tracker

Read `docs/todo.md` at the start of every session. It tracks all implementation progress across phases. When starting work, pick up from where the last session left off — find the first unchecked item and continue from there. Update `docs/todo.md` as tasks are completed.
